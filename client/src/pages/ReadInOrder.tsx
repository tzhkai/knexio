/** Style note: Field Notes for Better Work — this is a staged desk manual, not a generic course catalog. */
import { ArrowDown, ArrowRight, BookOpenCheck, CheckCircle2, Compass, FileStack, Flag, Route, ShieldCheck } from "lucide-react";
import { Link } from "wouter";
import Layout from "@/components/Layout";
import { SeoMeta } from "@/components/SeoMeta";
import SiteBreadcrumb, { breadcrumbListSchema } from "@/components/SiteBreadcrumb";
import { guides, learningPath } from "@/lib/content";

const pathStyles = `
  .path-hero { display:grid; grid-template-columns:minmax(0,1fr) 295px; gap:8vw; width:min(1170px,calc(100% - 48px)); margin:0 auto; padding:76px 0 61px; border-bottom:1px solid var(--ink); } .path-hero h1 { max-width:760px; margin:12px 0 17px; font-family:"DM Serif Display",Georgia,serif; font-size:clamp(54px,6vw,86px); font-weight:400; letter-spacing:-.065em; line-height:.92; } .path-hero p { max-width:685px; margin:0; color:#595b53; font-size:15px; line-height:1.8; } .path-side-record { align-self:end; padding:18px 0 0 19px; border-left:1px solid var(--green); } .path-side-record strong { display:block; color:var(--green); font-size:9px; letter-spacing:.1em; text-transform:uppercase; } .path-side-record p { margin:8px 0 0; color:#60635b; font-size:12px; line-height:1.65; } .path-summary { display:grid; grid-template-columns:repeat(3,1fr); width:min(1170px,calc(100% - 48px)); margin:0 auto; border-left:1px solid var(--rule); } .path-summary div { min-height:112px; padding:20px 22px; border-right:1px solid var(--rule); border-bottom:1px solid var(--rule); } .path-summary strong { display:block; color:var(--rust); font-family:"DM Serif Display",Georgia,serif; font-size:29px; letter-spacing:-.06em; line-height:.9; } .path-summary span { display:block; margin-top:7px; color:#5b5d55; font-size:11px; font-weight:700; line-height:1.45; } .path-jump { display:flex; align-items:center; flex-wrap:wrap; gap:9px; width:min(1170px,calc(100% - 48px)); margin:27px auto 0; } .path-jump>span { color:var(--green); font-size:9px; font-weight:800; letter-spacing:.1em; text-transform:uppercase; } .path-jump a { padding:7px 9px; border-left:1px solid var(--green); color:#49675c; font-size:10px; font-weight:800; letter-spacing:.05em; text-transform:uppercase; transition:background 160ms var(--ease-out),color 160ms var(--ease-out); } .path-jump a:hover { background:var(--green); color:#fffdf8; } .path-intro { display:grid; grid-template-columns:.75fr 1.25fr; gap:10vw; width:min(1070px,calc(100% - 48px)); margin:0 auto; padding:76px 0 53px; } .path-intro h2 { margin:10px 0 0; font-family:"DM Serif Display",Georgia,serif; font-size:40px; font-weight:400; letter-spacing:-.045em; line-height:1.04; } .path-intro p { margin:0 0 13px; color:#575951; font-size:14px; line-height:1.82; } .path-stages { width:min(1170px,calc(100% - 48px)); margin:0 auto; padding-bottom:114px; } .path-stage { display:grid; grid-template-columns:230px minmax(0,1fr); gap:48px; padding:35px 0 49px; border-top:1px solid var(--ink); scroll-margin-top:104px; } .path-stage-rail { position:sticky; top:34px; align-self:start; } .path-stage-number { color:var(--rust); font-family:"DM Serif Display",Georgia,serif; font-size:59px; letter-spacing:-.08em; line-height:.75; } .path-stage-rail h2 { margin:17px 0 9px; font-family:"DM Serif Display",Georgia,serif; font-size:30px; font-weight:400; letter-spacing:-.045em; line-height:1.04; } .path-stage-rail p { margin:0; color:#5f6259; font-size:12px; line-height:1.68; } .path-stage-note { display:flex; align-items:flex-start; gap:7px; margin-top:17px; color:var(--green); font-size:10px; font-weight:800; line-height:1.5; } .path-stage-note svg { flex:0 0 auto; margin-top:1px; } .path-records { border-top:1px solid var(--rule); } .path-record { position:relative; display:grid; grid-template-columns:39px minmax(0,1fr) auto; gap:15px; align-items:start; padding:20px 16px 20px 0; border-bottom:1px solid var(--rule); color:var(--ink); transition:background 160ms var(--ease-out),padding-left 160ms var(--ease-out); } .path-record:hover { padding-left:10px; background:var(--green-pale); } .path-record-index { color:var(--rust); font-family:"DM Serif Display",Georgia,serif; font-size:21px; letter-spacing:-.05em; line-height:.95; } .path-record-meta { display:flex; flex-wrap:wrap; gap:7px; margin-bottom:6px; color:var(--green); font-size:9px; font-weight:800; letter-spacing:.08em; text-transform:uppercase; } .path-record h3 { margin:0 0 6px; font-family:"DM Serif Display",Georgia,serif; font-size:25px; font-weight:400; letter-spacing:-.035em; line-height:1.06; } .path-record p { max-width:670px; margin:0; color:#5c5f57; font-size:12px; line-height:1.62; } .path-record svg { align-self:center; color:var(--green); transition:transform 160ms var(--ease-out); } .path-record:hover>svg { transform:translateX(3px); } .path-closing { display:grid; grid-template-columns:23px minmax(0,1fr); gap:13px; max-width:720px; margin:18px 0 0 auto; padding:19px 20px; border-left:2px solid var(--green); background:var(--green-pale); color:#3b5349; } .path-closing p { margin:0; font-size:12px; line-height:1.65; } @media (max-width:760px) { .path-hero,.path-summary,.path-intro,.path-stages,.path-jump { width:min(100% - 28px,760px); } .path-hero { grid-template-columns:1fr; gap:27px; padding:51px 0 45px; } .path-hero h1 { font-size:49px; } .path-hero p { font-size:14px; } .path-summary { grid-template-columns:1fr; } .path-summary div { min-height:0; padding:17px 18px; } .path-jump { gap:6px; margin-top:19px; } .path-jump>span { width:100%; margin-bottom:2px; } .path-intro { grid-template-columns:1fr; gap:22px; padding:57px 0 36px; } .path-intro h2 { font-size:34px; } .path-stages { padding-bottom:80px; } .path-stage { grid-template-columns:1fr; gap:24px; padding:27px 0 38px; } .path-stage-rail { position:static; } .path-stage-number { font-size:47px; } .path-stage-rail h2 { margin-top:12px; font-size:29px; } .path-record { grid-template-columns:31px minmax(0,1fr); gap:9px; padding:17px 5px 17px 0; } .path-record h3 { font-size:23px; } .path-record>svg { grid-column:2; justify-self:end; margin-top:-22px; } .path-record p { padding-right:15px; } .path-closing { margin-top:10px; } }
`;

export default function ReadInOrder() {
  const stages = learningPath.stages.map(stage => ({ ...stage, guides: stage.guideSlugs.map(slug => guides.find(guide => guide.slug === slug)).filter(Boolean) }));
  const totalRecords = stages.reduce((total, stage) => total + stage.guides.length, 0);
  return <Layout><SeoMeta title="Read AI workflow guides in order" description="A four-stage reading path through Workflow Library: frame work, make decisions visible, run the conversation, and carry the record forward." schema={({ origin, pageUrl }) => ([{ "@type": "ItemList", name: "Workflow Library — Read in order", numberOfItems: totalRecords, itemListOrder: "https://schema.org/ItemListOrderAscending", itemListElement: stages.flatMap(stage => stage.guides).map((guide, index) => ({ "@type": "ListItem", position: index + 1, name: guide!.title, url: `${origin}/guides/${guide!.slug}` })) }, breadcrumbListSchema(origin, pageUrl, [{ label: "Home", href: "/" }, { label: "Read in order" }])])} /><style>{pathStyles}</style><SiteBreadcrumb items={[{ label: "Home", href: "/" }, { label: "Read in order" }]} />
    <section className="path-hero"><div><span className="eyebrow">Series tutorial / 01–{String(totalRecords).padStart(2, "0")}</span><h1>Read the library like a working system.</h1><p>This is a practical sequence for moving from a vague task to a visible record, a focused conversation, and a handoff another person can use. Every guide still stands on its own; read a stage only when the work in front of you calls for it.</p></div><aside className="path-side-record"><strong>How to use this path</strong><p>Start with the stage closest to your current task. Use the record links, confirm the boundaries, and let a human owner decide what happens next.</p></aside></section>
    <section className="path-summary" aria-label="Reading path summary"><div><strong>04</strong><span>stages of practical work</span></div><div><strong>{String(totalRecords).padStart(2, "0")}</strong><span>reviewed workflow records</span></div><div><strong>0</strong><span>promises of automatic outcomes</span></div></section>
    <nav className="path-jump" aria-label="Jump to a reading stage"><span>Jump to a stage</span>{stages.map(stage => <a href={`#${stage.slug}`} key={stage.slug}>{stage.number} — {stage.shortTitle}</a>)}</nav>
    <section className="path-intro"><div><span className="eyebrow">A sequence, not a syllabus</span><h2>Build a trail the next person can inspect.</h2></div><div><p>The path begins by giving a task enough context to be useful. It then makes decisions and constraints visible, converts discussion into clear follow-up, and preserves the record for the next period of work. The aim is not to automate responsibility. It is to make responsibility easier to see.</p><p>Each stage includes a small number of guides because long checklists do not become useful merely by being called a curriculum. Pause when your situation changes; return to the library when you need another entry point.</p></div></section>
    <section className="path-stages">{stages.map(stage => <section className="path-stage" id={stage.slug} key={stage.slug}><aside className="path-stage-rail"><span className="path-stage-number">{stage.number}</span><span className="eyebrow">{stage.label}</span><h2>{stage.title}</h2><p>{stage.description}</p><span className="path-stage-note"><Flag size={14} /> {stage.outcome}</span></aside><div><div className="path-records">{stage.guides.map((guide, index) => <Link className="path-record" href={`/guides/${guide!.slug}`} key={guide!.slug}><span className="path-record-index">{String(index + 1).padStart(2, "0")}</span><div><span className="path-record-meta">{guide!.category} · {guide!.readingTime}</span><h3>{guide!.title}</h3><p>{guide!.dek}</p></div><ArrowRight size={18} aria-hidden="true" /></Link>)}</div>{stage.closing && <div className="path-closing"><ShieldCheck size={18} /><p>{stage.closing}</p></div>}</div></section>)}</section>
    <section className="path-closing"><Route size={19} /><p>Finished a stage? Continue in the next one only when the work needs it. You can also return to the <Link href="/guides">full library</Link> to choose a different task entry point.</p></section>
  </Layout>;
}
