import { useMemo, useState } from "react";
import { ArrowUpRight, Check, Clipboard, FileText, RotateCcw, Sparkles } from "lucide-react";
import { Link } from "wouter";
import Layout from "@/components/Layout";
import { SeoMeta } from "@/components/SeoMeta";
import SiteBreadcrumb, { breadcrumbListSchema } from "@/components/SiteBreadcrumb";

const toolStyles = `
.tool-page{max-width:1180px;margin:0 auto;padding:92px 34px 110px;color:var(--ink)}
.tool-kicker{display:flex;align-items:center;gap:9px;color:var(--green);font-size:10px;font-weight:800;letter-spacing:.14em;text-transform:uppercase}
.tool-hero{max-width:760px;margin:26px 0 42px}.tool-hero h1{margin:0;font-family:"DM Serif Display",Georgia,serif;font-size:clamp(44px,7vw,76px);font-weight:400;letter-spacing:-.065em;line-height:.96}.tool-hero h1 em{color:var(--green);font-style:italic}.tool-hero p{max-width:660px;margin:25px 0 0;color:#5d625c;font-size:17px;line-height:1.7}
.tool-workbench{display:grid;grid-template-columns:minmax(0,1.15fr) minmax(320px,.85fr);gap:18px;align-items:stretch}.tool-panel{min-width:0;border:1px solid #c9c9bb;background:#f8f7f0;padding:22px;box-shadow:8px 8px 0 rgba(23,107,91,.08)}.tool-panel.dark{background:#103f36;color:#f4f0e5;border-color:#103f36;box-shadow:none}.tool-panel h2{margin:0 0 17px;font-family:"DM Serif Display",Georgia,serif;font-size:27px;font-weight:400;letter-spacing:-.035em}.tool-panel.dark h2{color:#f4f0e5}.tool-label{display:block;margin:0 0 8px;color:inherit;font-size:11px;font-weight:800;letter-spacing:.1em;text-transform:uppercase}.tool-input{display:block;width:100%;min-height:280px;resize:vertical;border:1px solid #b8b9ab;background:#fffef8;padding:16px;color:#26312d;font:500 14px/1.65 Manrope,system-ui,sans-serif;outline:none}.tool-input:focus{border-color:var(--green);box-shadow:0 0 0 3px rgba(23,107,91,.12)}.tool-panel.dark .tool-input{background:#f8f7f0}.tool-actions{display:flex;flex-wrap:wrap;gap:9px;margin-top:13px}.tool-button{display:inline-flex;align-items:center;justify-content:center;gap:7px;border:1px solid var(--green);background:var(--green);color:#fff;padding:11px 15px;font-size:12px;font-weight:800;cursor:pointer;transition:transform .16s ease,background .16s ease}.tool-button:hover{background:#0f594b}.tool-button:active{transform:scale(.97)}.tool-button.secondary{background:transparent;color:var(--green)}.tool-panel.dark .tool-button.secondary{color:#e7d5b6;border-color:#b66b4d}.tool-stats{display:grid;grid-template-columns:repeat(3,1fr);gap:8px;margin-top:13px}.tool-stat{border-top:1px solid #d1d0c2;padding-top:9px}.tool-stat strong{display:block;font-family:"DM Serif Display",Georgia,serif;font-size:27px;font-weight:400}.tool-stat span{display:block;color:#6c7068;font-size:10px;text-transform:uppercase;letter-spacing:.08em}.tool-panel.dark .tool-stat{border-color:rgba(255,255,255,.22)}.tool-panel.dark .tool-stat span{color:#c9d4cb}.tool-preview{min-height:280px;border:1px solid rgba(255,255,255,.25);background:#f8f7f0;color:#28352e;padding:18px;overflow:auto}.tool-preview h1,.tool-preview h2,.tool-preview h3{font-family:"DM Serif Display",Georgia,serif;font-weight:400;line-height:1.05}.tool-preview h1{font-size:31px}.tool-preview h2{font-size:25px}.tool-preview h3{font-size:20px}.tool-preview p,.tool-preview li{font-size:14px;line-height:1.7}.tool-preview ul{padding-left:22px}.tool-preview blockquote{margin:15px 0;border-left:3px solid #b66b4d;padding-left:14px;color:#5d625c}.tool-preview code{background:#e7e5d8;padding:2px 5px;font:12px ui-monospace,SFMono-Regular,monospace}.tool-preview pre{overflow:auto;background:#163f36;color:#eef1e8;padding:13px}.tool-preview pre code{background:none;color:inherit}.tool-preview a{color:var(--green);text-decoration:underline}.tool-note{margin:15px 0 0;color:#7a8178;font-size:11px;line-height:1.55}.tool-panel.dark .tool-note{color:#c9d4cb}.tool-lower{display:grid;grid-template-columns:1.1fr .9fr;gap:18px;margin-top:56px}.tool-copy{border-top:1px solid var(--ink);padding-top:14px}.tool-copy h2{margin:0 0 11px;font-family:"DM Serif Display",Georgia,serif;font-size:32px;font-weight:400}.tool-copy p{margin:0;color:#5d625c;line-height:1.75}.tool-related{display:grid;gap:10px}.tool-related a{display:flex;justify-content:space-between;gap:10px;border-bottom:1px solid #cbcbbd;padding:11px 0;color:var(--ink);font-size:13px;font-weight:700}.tool-related a svg{color:var(--green);flex:none}.tool-privacy{margin-top:18px;padding:14px 16px;background:#e9eee8;color:#536259;font-size:11px;line-height:1.55}.tool-privacy strong{color:var(--green)}
@media(max-width:820px){.tool-page{padding:62px 20px 80px}.tool-workbench,.tool-lower{grid-template-columns:1fr}.tool-hero p{font-size:15px}.tool-panel{padding:17px}.tool-input,.tool-preview{min-height:240px}}
@media(max-width:520px){.tool-page{padding-left:16px;padding-right:16px}.tool-hero h1{font-size:47px}.tool-stats{gap:5px}.tool-stat strong{font-size:23px}.tool-button{width:100%}}
`;

function encode(value: string) { return value.replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;").replace(/\"/g, "&quot;"); }
function inlineMarkdown(value: string) { return value.replace(/`([^`]+)`/g, "<code>$1</code>").replace(/\*\*([^*]+)\*\*/g, "<strong>$1</strong>").replace(/\*([^*]+)\*/g, "<em>$1</em>"); }
export function countPromptWords(source: string) { return source.trim() ? source.trim().split(/\s+/).length : 0; }
export function renderMarkdown(source: string) {
  const lines = source.split(/\r?\n/); const output: string[] = []; let list = false; let code = false; let codeLines: string[] = [];
  const closeList = () => { if (list) { output.push("</ul>"); list = false; } };
  lines.forEach((line) => { if (line.trim().startsWith("```")) { if (code) { output.push(`<pre><code>${encode(codeLines.join("\n"))}</code></pre>`); codeLines = []; code = false; } else { closeList(); code = true; } return; } if (code) { codeLines.push(line); return; } const raw = line.trimEnd(); if (!raw.trim()) { closeList(); return; } const safe = encode(raw); if (/^### /.test(raw)) { closeList(); output.push(`<h3>${inlineMarkdown(encode(raw.slice(4)))}</h3>`); } else if (/^## /.test(raw)) { closeList(); output.push(`<h2>${inlineMarkdown(encode(raw.slice(3)))}</h2>`); } else if (/^# /.test(raw)) { closeList(); output.push(`<h1>${inlineMarkdown(encode(raw.slice(2)))}</h1>`); } else if (/^[-*] /.test(raw)) { if (!list) { output.push("<ul>"); list = true; } output.push(`<li>${inlineMarkdown(encode(raw.slice(2)))}</li>`); } else if (/^> /.test(raw)) { closeList(); output.push(`<blockquote>${inlineMarkdown(encode(raw.slice(2)))}</blockquote>`); } else { closeList(); output.push(`<p>${inlineMarkdown(safe)}</p>`); } }); closeList(); if (code) output.push(`<pre><code>${encode(codeLines.join("\n"))}</code></pre>`); return { __html: output.join("") };
}

function ToolFrame({ title, intro, children, related }: { title: React.ReactNode; intro: string; children: React.ReactNode; related: { href: string; label: string }[] }) {
  return <Layout><style>{toolStyles}</style><div className="tool-page"><SiteBreadcrumb items={[{ label: "Home", href: "/" }, { label: "Workflow utilities" }]} /><div className="tool-kicker"><Sparkles size={14} /> Workflow utility · runs in your browser</div><header className="tool-hero"><h1>{title}</h1><p>{intro}</p></header>{children}<div className="tool-lower"><section className="tool-copy"><h2>Why this belongs in the workflow.</h2><p>Small tools are useful when they make a review step clearer, not when they promise to replace judgment. This utility keeps the work visible and processes the input locally in your browser.</p><div className="tool-privacy"><strong>Privacy note.</strong> The current version does not upload your text to a server. Avoid pasting confidential material into any browser tool unless you understand the device and browser you are using.</div></section><nav className="tool-related" aria-label="Related Workflow Library guides">{related.map(item => <Link key={item.href} href={item.href}>{item.label}<ArrowUpRight size={15} /></Link>)}</nav></div></div></Layout>;
}

export function AiPromptWordCounter() {
  const [text, setText] = useState(""); const [copied, setCopied] = useState(false); const words = useMemo(() => countPromptWords(text), [text]); const chars = text.length; const lines = text ? text.split(/\r?\n/).length : 0;
  return <ToolFrame title={<>Count the words before you <em>prompt.</em></>} intro="A calm, local word and character counter for shaping AI prompts before you send them. It counts the text you provide; it does not claim to calculate model-specific tokens." related={[{ href: "/guides/brief-first-prompt-pattern", label: "Use the brief-first prompt pattern" }, { href: "/guides/clear-project-update-prompt", label: "Draft a clearer project update" }]}><SeoMeta title="AI Prompt Word Counter — Count Words and Characters | Workflow Library" description="Count words, characters, and lines in an AI prompt locally in your browser before you send it." schema={({ origin, pageUrl }: { origin: string; pageUrl: string }) => ({ "@context": "https://schema.org", "@type": "WebApplication", name: "AI Prompt Word Counter", url: pageUrl, applicationCategory: "UtilitiesApplication", operatingSystem: "Any", offers: { "@type": "Offer", price: "0", priceCurrency: "USD" }, isPartOf: breadcrumbListSchema(origin, pageUrl, [{ label: "Home", href: "/" }, { label: "Workflow utilities" }]) })} /><section className="tool-workbench"><div className="tool-panel"><h2>Prompt scratchpad</h2><label className="tool-label" htmlFor="prompt-counter-input">Paste or write your prompt</label><textarea id="prompt-counter-input" className="tool-input" value={text} onChange={event => setText(event.target.value)} placeholder="Task: ...\nContext: ...\nConstraints: ...\nOutput: ..." /><div className="tool-actions"><button className="tool-button" type="button" onClick={async () => { await navigator.clipboard?.writeText(text); setCopied(true); window.setTimeout(() => setCopied(false), 1600); }}>{copied ? <Check size={15} /> : <Clipboard size={15} />} {copied ? "Copied" : "Copy prompt"}</button><button className="tool-button secondary" type="button" onClick={() => setText("")}><RotateCcw size={15} /> Clear</button></div><div className="tool-stats"><div className="tool-stat"><strong>{words}</strong><span>Words</span></div><div className="tool-stat"><strong>{chars}</strong><span>Characters</span></div><div className="tool-stat"><strong>{lines}</strong><span>Lines</span></div></div></div><div className="tool-panel dark"><h2>Use the count as a check.</h2><p className="tool-note">A longer prompt is not automatically a better prompt. Look for the smallest context and constraint set that lets another person review the request.</p><div className="tool-preview" style={{ marginTop: 28 }}><p><strong>Brief-first check</strong></p><ul><li>Is the task specific?</li><li>Is the audience named?</li><li>Are constraints visible?</li><li>Can the output be reviewed?</li></ul></div></div></section></ToolFrame>;
}

export function MarkdownPreview() {
  const [markdown, setMarkdown] = useState("# A reviewable first draft\n\nUse **context**, constraints, and a clear output shape.\n\n- Keep sources visible\n- Label uncertainty\n- Check the final result\n\n> A useful workflow leaves you with more clarity.\n\n`brief-first`"); const rendered = useMemo(() => renderMarkdown(markdown), [markdown]); const [copied, setCopied] = useState(false);
  return <ToolFrame title={<>Preview the work before you <em>publish.</em></>} intro="A lightweight Markdown preview for notes, prompts, and first drafts. Rendering happens locally in your browser with a deliberately small, safe Markdown subset." related={[{ href: "/guides/brief-first-prompt-pattern", label: "Use the brief-first prompt pattern" }, { href: "/guides/research-brief-from-scattered-sources", label: "Turn scattered sources into a research brief" }]}><SeoMeta title="Markdown Preview — Preview Notes and Prompts Locally | Workflow Library" description="Preview Markdown notes, prompts, and first drafts locally in your browser with a simple, safe writing surface." schema={({ origin, pageUrl }: { origin: string; pageUrl: string }) => ({ "@context": "https://schema.org", "@type": "WebApplication", name: "Markdown Preview", url: pageUrl, applicationCategory: "UtilitiesApplication", operatingSystem: "Any", offers: { "@type": "Offer", price: "0", priceCurrency: "USD" }, isPartOf: breadcrumbListSchema(origin, pageUrl, [{ label: "Home", href: "/" }, { label: "Workflow utilities" }]) })} /><section className="tool-workbench"><div className="tool-panel"><h2>Markdown input</h2><label className="tool-label" htmlFor="markdown-preview-input">Write or paste Markdown</label><textarea id="markdown-preview-input" className="tool-input" value={markdown} onChange={event => setMarkdown(event.target.value)} /><div className="tool-actions"><button className="tool-button" type="button" onClick={async () => { await navigator.clipboard?.writeText(markdown); setCopied(true); window.setTimeout(() => setCopied(false), 1600); }}>{copied ? <Check size={15} /> : <Clipboard size={15} />} {copied ? "Copied" : "Copy Markdown"}</button><button className="tool-button secondary" type="button" onClick={() => setMarkdown("")}><RotateCcw size={15} /> Clear</button></div><p className="tool-note"><FileText size={13} style={{ verticalAlign: "-2px" }} /> Supports headings, emphasis, lists, quotes, inline code, and fenced code blocks.</p></div><div className="tool-panel dark"><h2>Rendered preview</h2><div className="tool-preview" dangerouslySetInnerHTML={rendered} /><p className="tool-note">Links and raw HTML are intentionally not interpreted in this local preview.</p></div></section></ToolFrame>;
}
