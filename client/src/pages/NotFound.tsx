/** Style note: Field Notes for Better Work — a missing route becomes a calm recovery desk with a useful next move. */
import { useMemo, useState } from "react";
import { ArrowLeft, ArrowUpRight, BookOpenCheck, Compass, Flag, Home, LibraryBig, Search, Sparkles } from "lucide-react";
import { Link, useLocation } from "wouter";
import Layout from "@/components/Layout";
import { SeoMeta } from "@/components/SeoMeta";
import SiteBreadcrumb, { breadcrumbListSchema } from "@/components/SiteBreadcrumb";
import GuideCard from "@/components/GuideCard";
import { guides } from "@/lib/content";
import { brokenLinkReportHref, guideTitleSuggestions, latestGuides, searchGuides } from "@/lib/guide-discovery";

const errorStyles = `
  .error-record { display:grid; grid-template-columns:180px minmax(0,1fr) 250px; gap:38px; width:min(1170px,calc(100% - 48px)); margin:0 auto; padding:74px 0 64px; border-bottom:1px solid var(--ink); } .error-code-wrap { position:relative; align-self:start; min-height:132px; } .error-code { position:relative; z-index:1; color:var(--rust); font-family:"DM Serif Display",Georgia,serif; font-size:clamp(82px,10vw,144px); letter-spacing:-.1em; line-height:.72; } .error-orbit { position:absolute; top:31px; left:24px; width:102px; height:74px; border:1px dashed rgba(23,107,91,.36); border-radius:50%; transform:rotate(-20deg); } .error-orbit span { position:absolute; display:block; width:10px; height:10px; border:1px solid var(--green); background:#fffdf8; border-radius:50%; } .error-orbit span:nth-child(1) { top:-5px; left:39px; } .error-orbit span:nth-child(2) { right:-5px; bottom:13px; background:var(--green-pale); } .error-orbit span:nth-child(3) { left:10px; bottom:-4px; width:7px; height:7px; border-color:var(--rust); background:#fff3e8; } .error-copy h1 { max-width:680px; margin:11px 0 16px; font-family:"DM Serif Display",Georgia,serif; font-size:clamp(45px,5vw,73px); font-weight:400; letter-spacing:-.06em; line-height:.96; } .error-copy p { max-width:595px; margin:0; color:#595b53; font-size:15px; line-height:1.76; } .error-actions { display:flex; flex-wrap:wrap; gap:9px; margin-top:25px; } .error-secondary { display:inline-flex; align-items:center; gap:7px; min-height:42px; padding:0 13px; border:1px solid var(--rule); color:var(--green); font-size:11px; font-weight:800; transition:background 160ms var(--ease-out),color 160ms var(--ease-out),transform 160ms var(--ease-out); } .error-secondary:hover { border-color:var(--green); background:var(--green-pale); } .error-secondary:active { transform:scale(.97); } .error-side-note { align-self:end; padding:17px 0 0 19px; border-left:1px solid var(--green); } .error-side-note strong { display:block; color:var(--green); font-size:9px; letter-spacing:.1em; text-transform:uppercase; } .error-side-note p { margin:8px 0 0; color:#62645c; font-size:12px; line-height:1.62; } .error-recovery,.error-search,.error-reading { width:min(1170px,calc(100% - 48px)); margin:0 auto; } .error-recovery { padding:32px 0; border-bottom:1px solid var(--rule); } .error-recovery-grid { display:grid; grid-template-columns:92px minmax(0,1fr) 30px; gap:18px; align-items:start; } .error-recovery-number,.error-search-number,.error-reading-head>span { color:var(--rust); font-family:"DM Serif Display",Georgia,serif; font-size:38px; letter-spacing:-.07em; line-height:.8; } .error-recovery h2,.error-search h2,.error-reading-head h2 { margin:6px 0 8px; font-family:"DM Serif Display",Georgia,serif; font-size:30px; font-weight:400; letter-spacing:-.045em; line-height:1.02; } .error-recovery p,.error-search p { max-width:620px; margin:0; color:#63655d; font-size:13px; line-height:1.65; } .error-recovery-links { display:grid; gap:8px; grid-column:3; grid-row:1 / span 2; min-width:230px; } .error-recovery-links a { display:flex; align-items:center; justify-content:space-between; gap:10px; padding:12px 13px; border:1px solid var(--rule); color:var(--ink); text-decoration:none; transition:border-color 160ms var(--ease-out),background 160ms var(--ease-out),transform 160ms var(--ease-out); } .error-recovery-links a:hover { border-color:var(--green); background:var(--green-pale); } .error-recovery-links a:active { transform:scale(.98); } .error-recovery-links span { display:flex; align-items:center; gap:8px; font-size:11px; font-weight:800; } .error-recovery-links small { color:#62645c; font-size:10px; text-align:right; } .error-search { display:grid; grid-template-columns:92px minmax(0,1fr); gap:18px; padding:38px 0 40px; border-bottom:1px solid var(--rule); } .error-search form { display:flex; gap:8px; max-width:700px; margin-top:17px; } .error-search label { position:relative; flex:1; display:flex; align-items:center; border:1px solid var(--rule); background:#fffdf8; } .error-search label svg { flex:0 0 auto; margin-left:13px; color:var(--green); } .error-search input { width:100%; min-width:0; padding:13px 13px 13px 9px; border:0; outline:0; background:transparent; color:var(--ink); font:inherit; font-size:13px; } .error-search input:focus-visible { box-shadow:0 0 0 2px var(--green); } .error-search button { flex:0 0 auto; min-height:44px; padding:0 15px; border:1px solid var(--green); background:var(--green); color:#fffdf8; font:inherit; font-size:11px; font-weight:800; cursor:pointer; } .error-search-results { grid-column:2; } .error-search-status { margin:17px 0 0 !important; color:var(--green) !important; font-size:11px !important; font-weight:800; } .error-search-results mark { padding:0 .08em; color:inherit; background:var(--green-pale); box-decoration-break:clone; -webkit-box-decoration-break:clone; } .error-search-results .guide-grid { margin-top:18px; } .error-search-empty { margin:17px 0 0; padding:16px; border-left:2px solid var(--rust); background:#fffaf2; color:#5f554b; font-size:13px; line-height:1.6; } .error-reading { padding:54px 0 112px; } .error-reading-head { display:grid; grid-template-columns:92px minmax(0,1fr) auto; gap:18px; align-items:end; padding-bottom:21px; border-bottom:1px solid var(--rule); } .error-reading-head h2 { margin-bottom:0; font-size:33px; } .error-reading-head p { max-width:290px; margin:0; color:#63655d; font-size:11px; line-height:1.58; } .error-reading .guide-grid { margin-top:22px; } .error-paths { display:flex; flex-wrap:wrap; gap:8px; margin-top:22px; } .error-paths a { display:inline-flex; align-items:center; gap:6px; padding:7px 9px; border-left:1px solid var(--green); color:#47665a; font-size:10px; font-weight:800; letter-spacing:.055em; text-transform:uppercase; } .error-paths a:hover { background:var(--green); color:#fffdf8; } @media (prefers-reduced-motion:no-preference) { .error-orbit { animation:error-orbit 9s linear infinite; } .error-orbit span { animation:error-stars 2.6s var(--ease-out) infinite alternate; } .error-orbit span:nth-child(2) { animation-delay:.6s; } .error-orbit span:nth-child(3) { animation-delay:1.1s; } @keyframes error-orbit { to { transform:rotate(340deg); } } @keyframes error-stars { to { transform:scale(1.45); } } } @media (max-width:760px) { .error-record,.error-recovery,.error-search,.error-reading { width:min(100% - 28px,760px); } .error-record { grid-template-columns:76px minmax(0,1fr); gap:16px; padding:51px 0 45px; } .error-code-wrap { min-height:70px; } .error-code { font-size:69px; } .error-orbit { top:14px; left:9px; width:57px; height:42px; } .error-orbit span { width:7px; height:7px; } .error-copy h1 { font-size:43px; } .error-copy p { font-size:14px; } .error-actions { margin-top:19px; } .error-side-note { grid-column:1 / -1; padding:13px 0 0 14px; } .error-recovery { padding:26px 0; } .error-recovery-grid { grid-template-columns:43px minmax(0,1fr); gap:11px; } .error-recovery-number,.error-search-number,.error-reading-head>span { font-size:29px; } .error-recovery h2,.error-search h2 { font-size:26px; } .error-recovery-links { grid-column:1 / -1; grid-row:auto; min-width:0; margin-top:4px; } .error-search { grid-template-columns:43px minmax(0,1fr); gap:11px; padding:30px 0; } .error-search form { flex-direction:column; } .error-search button { width:100%; } .error-search-results { grid-column:1 / -1; } .error-reading { padding:43px 0 82px; } .error-reading-head { grid-template-columns:43px minmax(0,1fr); gap:11px; padding-bottom:17px; } .error-reading-head h2 { font-size:27px; } .error-reading-head p { grid-column:2; margin-top:3px; } .error-reading .guide-grid { margin-top:17px; } }
  .error-recovery-grid { grid-template-columns:92px minmax(0,1fr) minmax(230px,320px); } @media (max-width:760px) { .error-recovery-grid { grid-template-columns:43px minmax(0,1fr); } }
`;

export const legacyRecoveryLinks = [
  { href: "/", label: "Return home", detail: "Start at the front desk", icon: Home },
  { href: "/guides", label: "Browse guides", detail: "Choose a real task", icon: LibraryBig },
  { href: "/read-order", label: "Read in order", detail: "Follow a guided path", icon: Compass },
] as const;

export default function NotFound() {
  const [pathname] = useLocation();
  const [query, setQuery] = useState("");
  const matchedGuides = useMemo(() => searchGuides(guides, query).slice(0, 4), [query]);
  const suggestions = useMemo(() => guideTitleSuggestions(guides, query), [query]);
  const latest = useMemo(() => latestGuides(guides, 4), []);
  const hasSearch = Boolean(query.trim());

  return <Layout><SeoMeta title="Page not found" description="This page is not in the Workflow Library index." noIndex schema={({ origin, pageUrl }) => breadcrumbListSchema(origin, pageUrl, [{ label: "Home", href: "/" }, { label: "Page not found" }])} /><style>{errorStyles}</style><SiteBreadcrumb items={[{ label: "Home", href: "/" }, { label: "Page not found" }]} />
    <section className="error-record"><div className="error-code-wrap"><div className="error-code" aria-hidden="true">404</div><div className="error-orbit" aria-hidden="true"><span /><span /><span /></div></div><div className="error-copy"><span className="eyebrow">Link recovery</span><h1>This page has moved on.</h1><p>You may have followed an older link, a saved bookmark, or an incomplete address. The current Workflow Library is still here, with practical guides organized around the work you need to do next.</p><div className="error-actions"><Link href="/" className="primary-button"><Home size={16} /> Return home</Link><Link href="/guides" className="error-secondary"><Search size={15} /> Browse guides</Link><a href={brokenLinkReportHref(pathname)} className="error-secondary"><Flag size={15} /> Report broken link</a></div></div><aside className="error-side-note"><strong>Old-link note</strong><p>Earlier tools, games, and experiments were not carried into this focused library. Start again with a current workflow instead of chasing an expired address.</p></aside></section>
    <section className="error-recovery" aria-labelledby="legacy-recovery-title"><div className="error-recovery-grid"><span className="error-recovery-number" aria-hidden="true">01</span><div><span className="eyebrow">A clear next step</span><h2 id="legacy-recovery-title">Pick your way back in.</h2><p>There is no need to decode the old address. Begin at the homepage, browse by task, or use a curated path when you want a sequence rather than a single article.</p></div><nav className="error-recovery-links" aria-label="Primary recovery links">{legacyRecoveryLinks.map(({ href, label, detail, icon: Icon }) => <Link key={href} href={href}><span><Icon size={15} /> {label}</span><small>{detail}</small></Link>)}</nav></div></section>
    <section className="error-search" aria-labelledby="error-search-title"><span className="error-search-number" aria-hidden="true">02</span><div><span className="eyebrow">Search the current library</span><h2 id="error-search-title">Find the work you came to do.</h2><p>Search current guide titles, summaries, categories, and topics. Title suggestions appear as you type; your words stay in this browser and this field does not send an analytics event.</p><form onSubmit={event => event.preventDefault()}><label><Search size={17} aria-hidden="true" /><span className="sr-only">Search current guides</span><input value={query} onChange={event => setQuery(event.target.value)} placeholder="Try “meeting notes” or “project update”" list="error-guide-suggestions" autoComplete="off" aria-describedby="error-search-help" /><datalist id="error-guide-suggestions">{suggestions.map(suggestion => <option key={suggestion} value={suggestion} />)}</datalist></label><button type="submit">Search guides</button></form><p id="error-search-help" className="sr-only">Suggestions are based only on current guide titles.</p></div>{hasSearch && <div className="error-search-results" aria-live="polite"><p className="error-search-status">{matchedGuides.length} current guide{matchedGuides.length === 1 ? "" : "s"} match “{query.trim()}”</p>{matchedGuides.length ? <div className="guide-grid">{matchedGuides.map(guide => <GuideCard key={guide.slug} guide={guide} highlightQuery={query} />)}</div> : <p className="error-search-empty">No current guide matches that phrase. Try the task, outcome, or topic rather than the old page name.</p>}</div>}</section>
    <section className="error-reading" aria-labelledby="error-reading-title"><div className="error-reading-head"><span>03</span><div><span className="eyebrow">Latest guides</span><h2 id="error-reading-title">Start with what was updated most recently.</h2></div><p>Sorted only by the guides’ recorded update dates. This is not a popularity claim or a behavioral recommendation.</p></div><div className="guide-grid">{latest.map(guide => <GuideCard key={guide.slug} guide={guide} />)}</div><nav className="error-paths" aria-label="Other recovery paths"><Link href="/workflows/research-and-decisions"><Compass size={14} /> Research & decisions</Link><Link href="/workflows/writing-and-updates"><ArrowUpRight size={14} /> Writing & updates</Link><Link href="/workflows/planning-and-priorities"><BookOpenCheck size={14} /> Planning & priorities</Link><Link href="/guides/brief-first-prompt-pattern"><Sparkles size={14} /> Start with a prompt</Link><Link href="/guides/meeting-follow-up-email"><ArrowLeft size={14} /> Meeting follow-up</Link></nav></section>
  </Layout>;
}
