import { Check, Copy, Download, FileSpreadsheet, FileText, Printer, X } from "lucide-react";
import { useCallback, useEffect, useRef, useState } from "react";
import { copyTextToClipboard } from "@/lib/clipboard";

export const PRIORITY_PLAN_TEMPLATE_URL = "/manus-storage/research-to-priority-plan-template_75c37156.xlsx";
export const PRIORITY_PLAN_BLANK_ROW_STORAGE_KEY = "knexio.priority-plan.blank-row-count.v1";
export const PRIORITY_PLAN_MARKDOWN_TEMPLATE = `# Research → Priority Plan

> Use this working record to carry source-aware research into one modest next priority. Replace every bracketed field with your real context. Do not invent certainty, urgency, capacity, approval, owners, or dates.

## Decision context

- **Decision question:** [What decision does this plan support?]
- **Decision owner:** [Who can confirm, defer, or revise the trade-off?]
- **Review date or condition:** [When should the priority be checked again?]
- **Known constraint:** [Time, dependency, approval, capacity, or external condition]

## Evidence notes

| Claim or observation | Source label / date | Evidence type | Support status | Limitation or counter-evidence | Next verification step | Priority impact |
| --- | --- | --- | --- | --- | --- | --- |
| [record] | [source / date] | [type] | [Supported / Mixed / Needs verification / Not supported] | [limit] | [specific check] | [Could change the decision / Important context / Low impact] |

## Priority record

| Priority / next move | Decision it supports | Evidence reference | Gap or limitation | Dependency | Owner | Review condition | Status |
| --- | --- | --- | --- | --- | --- | --- |
| [smallest useful move] | [decision] | [source label] | [gap] | [dependency] | [owner or Not stated] | [condition] | [Not started / In review / Needs verification / Ready for decision / Deferred] |

## Human review before commitment

- [ ] Decision-critical evidence can be traced to a source label and date.
- [ ] Important gaps and dependencies are visible.
- [ ] The first priority is small enough to review, test, or reverse.
- [ ] The decision owner has confirmed the next review point.
`;

export const PRIORITY_PLAN_MARKDOWN_TABLE = `## Priority record

| Priority / next move | Decision it supports | Evidence reference | Gap or limitation | Dependency | Owner | Review condition | Status |
| --- | --- | --- | --- | --- | --- | --- |
| [smallest useful move] | [decision] | [source label] | [gap] | [dependency] | [owner or Not stated] | [condition] | [Not started / In review / Needs verification / Ready for decision / Deferred] |`;

export const PRIORITY_PLAN_CSV_COLUMNS = ["Priority / next move", "Decision it supports", "Evidence reference", "Gap or limitation", "Dependency", "Owner", "Review condition", "Status"] as const;
export const PRIORITY_PLAN_CSV_ROW = ["[smallest useful move]", "[decision]", "[source label]", "[gap]", "[dependency]", "[owner or Not stated]", "[condition]", "[Not started / In review / Needs verification / Ready for decision / Deferred]"] as const;

export function normalizeBlankRowCount(value: number) { return Number.isFinite(value) ? Math.min(25, Math.max(1, Math.floor(value))) : 6; }
export function readBlankRowPreference(storage: Pick<Storage, "getItem"> | null | undefined) { try { const saved = storage?.getItem(PRIORITY_PLAN_BLANK_ROW_STORAGE_KEY); return saved === null || saved === undefined ? null : normalizeBlankRowCount(Number(saved)); } catch { return null; } }
export function writeBlankRowPreference(storage: Pick<Storage, "setItem"> | null | undefined, count: number) { try { if (!storage) return false; storage.setItem(PRIORITY_PLAN_BLANK_ROW_STORAGE_KEY, String(normalizeBlankRowCount(count))); return true; } catch { return false; } }
export function normalizeProjectName(value: string) { return value.trim().replace(/\s+/g, " ").slice(0, 80); }
export function buildPriorityPlanFileName(projectName: string, extension: "xlsx" | "csv" | "md", suffix = "") {
  const slug = normalizeProjectName(projectName).toLowerCase().replace(/[^a-z0-9]+/g, "-").replace(/^-+|-+$/g, "") || "research-to-priority";
  return `${slug}-plan${suffix}.${extension}`;
}
export function formatPrintDate(date: Date) { return new Intl.DateTimeFormat("en-US", { year: "numeric", month: "long", day: "numeric" }).format(date); }
export function buildPriorityPlanCsv(rows: readonly (readonly string[])[] = [PRIORITY_PLAN_CSV_COLUMNS, PRIORITY_PLAN_CSV_ROW]) { const escape = (value: string) => `"${value.replaceAll('"', '""')}"`; return rows.map((row) => row.map(escape).join(",")).join("\r\n"); }
export function buildPriorityPlanCsvWithBlankRows(blankRowCount = 6) { const blankRow = PRIORITY_PLAN_CSV_COLUMNS.map(() => ""); return buildPriorityPlanCsv([PRIORITY_PLAN_CSV_COLUMNS, ...Array.from({ length: normalizeBlankRowCount(blankRowCount) }, () => blankRow)]); }

const styles = `
  .priority-plan-template { margin:30px 0; padding:21px 0; border-top:1px solid var(--ink); border-bottom:1px solid var(--rule); }.priority-plan-template-head { display:flex; align-items:flex-start; justify-content:space-between; gap:20px; }.priority-plan-template-kicker { display:inline-flex; align-items:center; gap:6px; color:var(--green); font-size:9px; font-weight:800; letter-spacing:.1em; text-transform:uppercase; }.priority-plan-template h2 { margin:7px 0; font-family:"DM Serif Display",Georgia,serif; font-size:28px; font-weight:400; letter-spacing:-.035em; line-height:1.08; }.priority-plan-template p { max-width:660px; margin:0; color:#5d625c; font-size:13px; line-height:1.65; }.priority-plan-template-note { display:block; margin-top:9px; color:#687169; font-size:10px; line-height:1.5; }
  .priority-plan-template-actions { display:grid; gap:8px; flex-shrink:0; }.priority-plan-template-button { display:inline-flex; align-items:center; justify-content:center; gap:7px; min-height:37px; padding:0 13px; border:1px solid var(--green); background:var(--green); color:#fffdf8; font-size:10px; font-weight:800; letter-spacing:.03em; transition:background 160ms var(--ease-out),transform 160ms var(--ease-out); }.priority-plan-template-button.is-secondary { background:transparent; color:var(--green); }.priority-plan-template-button:hover { background:var(--green-deep); }.priority-plan-template-button.is-secondary:hover { background:var(--green-pale); }.priority-plan-template-button:active { transform:scale(.97); }.priority-plan-template-button:focus-visible { outline:3px solid #b66b4d; outline-offset:4px; }.priority-plan-template-field { display:grid; gap:4px; padding:8px; border:1px solid var(--rule); color:#5f6d64; font-size:9px; font-weight:800; letter-spacing:.03em; }.priority-plan-template-field select,.priority-plan-template-field input { min-height:30px; border:1px solid var(--rule); background:#fffdf8; color:var(--ink); font:inherit; font-size:11px; padding:0 7px; }.priority-plan-template-field input { width:100%; }.priority-plan-template-field select:focus-visible,.priority-plan-template-field input:focus-visible { outline:3px solid #b66b4d; outline-offset:2px; }
  .priority-plan-template-toast { position:fixed; z-index:110; right:20px; bottom:20px; display:flex; align-items:center; gap:8px; max-width:min(340px,calc(100vw - 40px)); padding:12px 14px; border:1px solid rgba(255,255,255,.18); background:#17342e; color:#fffdf8; box-shadow:0 13px 30px rgba(24,43,36,.2); font-size:12px; font-weight:800; line-height:1.4; transition:transform 180ms var(--ease-out),opacity 180ms var(--ease-out); }.priority-plan-template-toast svg { flex-shrink:0; color:#a6d6c8; }
  .priority-plan-print-preview { position:fixed; z-index:120; inset:0; display:grid; place-items:center; padding:24px; background:rgba(19,41,35,.6); }.priority-plan-print-sheet { width:min(100%,940px); max-height:calc(100vh - 48px); overflow:auto; padding:34px; background:#fffdf8; color:#172c26; box-shadow:0 24px 70px rgba(10,30,24,.35); outline:0; }.priority-plan-print-head { display:flex; align-items:flex-start; justify-content:space-between; gap:16px; border-bottom:2px solid #176b5b; padding-bottom:16px; }.priority-plan-print-head h2 { margin:0; font-family:"DM Serif Display",Georgia,serif; font-size:32px; line-height:1.1; }.priority-plan-print-head p { margin-top:8px; color:#4e5d54; }.priority-plan-print-custom-heading { margin:8px 0 0; color:#176b5b !important; font-size:13px !important; font-weight:800; letter-spacing:.03em; }.priority-plan-print-meta { display:grid; justify-items:end; gap:10px; text-align:right; }.priority-plan-print-date { color:#506257; font-size:11px; font-weight:800; white-space:nowrap; }.priority-plan-print-close { display:grid; place-items:center; width:32px; height:32px; border:1px solid #cfd3c9; background:#fffdf8; color:#25483d; }.priority-plan-print-close:focus-visible { outline:3px solid #b66b4d; outline-offset:3px; }.priority-plan-print-table { width:100%; border-collapse:collapse; margin-top:22px; font-size:11px; }.priority-plan-print-table th,.priority-plan-print-table td { border:1px solid #9ca69e; padding:9px; vertical-align:top; text-align:left; }.priority-plan-print-table th { background:#176b5b; color:#fffdf8; font-weight:800; }.priority-plan-print-table td { height:54px; }.priority-plan-print-checklist { margin:22px 0 0; padding-left:18px; font-size:12px; line-height:1.7; }.priority-plan-print-actions { display:flex; justify-content:flex-end; gap:9px; margin-top:22px; }.priority-plan-print-actions button { min-height:38px; padding:0 13px; border:1px solid #176b5b; background:#176b5b; color:#fffdf8; font:inherit; font-size:11px; font-weight:800; }.priority-plan-print-actions button.is-secondary { background:#fffdf8; color:#176b5b; }
  @media(max-width:760px) { .priority-plan-template-head { display:grid; gap:13px; }.priority-plan-template h2 { font-size:25px; }.priority-plan-template-actions { grid-template-columns:1fr; }.priority-plan-template-button { width:100%; }.priority-plan-template p { font-size:12px; }.priority-plan-template-toast { right:14px; bottom:14px; max-width:calc(100vw - 28px); }.priority-plan-print-preview { padding:12px; }.priority-plan-print-sheet { max-height:calc(100vh - 24px); padding:20px; }.priority-plan-print-head h2 { font-size:27px; }.priority-plan-print-table { min-width:740px; }.priority-plan-print-sheet { overflow:auto; } } @media(prefers-reduced-motion:reduce) { .priority-plan-template-toast { transition:none; } } @media print { body * { visibility:hidden !important; } .priority-plan-print-preview,.priority-plan-print-preview * { visibility:visible !important; } .priority-plan-print-preview { position:fixed; inset:0; display:block; padding:0; background:#fff; } .priority-plan-print-sheet { width:100%; max-height:none; overflow:visible; padding:12mm; box-shadow:none; } .priority-plan-print-close,.priority-plan-print-actions { display:none !important; } .priority-plan-print-table { font-size:9px; } .priority-plan-print-table th,.priority-plan-print-table td { padding:5px; } }
`;

function startLocalDownload(filename: string, content: string, type: string) { const blob = new Blob([content], { type }); const url = URL.createObjectURL(blob); const anchor = document.createElement("a"); anchor.href = url; anchor.download = filename; document.body.appendChild(anchor); anchor.click(); anchor.remove(); URL.revokeObjectURL(url); }
function PriorityPlanPrintPreview({ onClose, customHeading, projectName, printDate }: { onClose: () => void; customHeading: string; projectName: string; printDate: Date }) { const previewRef = useRef<HTMLDivElement>(null); useEffect(() => { previewRef.current?.focus(); }, []); const title = projectName ? `${projectName} priority plan` : "Research-to-priority plan"; return <div className="priority-plan-print-preview" role="dialog" aria-modal="true" aria-labelledby="priority-plan-print-title" onKeyDown={(event) => { if (event.key === "Escape") onClose(); }}><div ref={previewRef} tabIndex={-1} className="priority-plan-print-sheet"><div className="priority-plan-print-head"><div><span className="priority-plan-template-kicker">Printable working record</span>{customHeading && <p className="priority-plan-print-custom-heading">{customHeading}</p>}<h2 id="priority-plan-print-title">{title}</h2><p>Use this blank record to keep evidence, limits, and a human review point visible before making a commitment.</p></div><div className="priority-plan-print-meta"><time className="priority-plan-print-date" dateTime={printDate.toISOString()}>{formatPrintDate(printDate)}</time><button type="button" className="priority-plan-print-close" onClick={onClose} aria-label="Close print preview"><X size={16} aria-hidden="true" /></button></div></div><table className="priority-plan-print-table"><thead><tr>{PRIORITY_PLAN_CSV_COLUMNS.map((column) => <th key={column}>{column}</th>)}</tr></thead><tbody>{Array.from({ length: 6 }, (_, index) => <tr key={index}>{PRIORITY_PLAN_CSV_COLUMNS.map((column) => <td key={column} />)}</tr>)}</tbody></table><ul className="priority-plan-print-checklist"><li>Decision-critical evidence can be traced to a source label and date.</li><li>Important gaps and dependencies are visible.</li><li>The first priority is small enough to review, test, or reverse.</li><li>The decision owner has confirmed the next review point.</li></ul><div className="priority-plan-print-actions"><button type="button" className="is-secondary" onClick={onClose}>Back to article</button><button type="button" onClick={() => window.print()}><Printer size={15} aria-hidden="true" /> Print this plan</button></div></div></div>; }

export default function PriorityPlanTemplate() {
  const [feedback, setFeedback] = useState(""); const [blankRowCount, setBlankRowCount] = useState(6); const [hasLoadedBlankRowPreference, setHasLoadedBlankRowPreference] = useState(false); const [printPreviewOpen, setPrintPreviewOpen] = useState(false); const [printHeading, setPrintHeading] = useState(""); const [projectName, setProjectName] = useState(""); const [printDate, setPrintDate] = useState<Date | null>(null); const feedbackTimer = useRef<number | null>(null);
  const normalizedProjectName = normalizeProjectName(projectName); const fileName = (extension: "xlsx" | "csv" | "md", suffix = "") => buildPriorityPlanFileName(normalizedProjectName, extension, suffix);
  const showFeedback = useCallback((message: string) => { if (feedbackTimer.current) window.clearTimeout(feedbackTimer.current); setFeedback(message); feedbackTimer.current = window.setTimeout(() => setFeedback(""), 2600); }, []);
  useEffect(() => () => { if (feedbackTimer.current) window.clearTimeout(feedbackTimer.current); }, []); useEffect(() => { const stored = readBlankRowPreference(window.localStorage); if (stored !== null) setBlankRowCount(stored); setHasLoadedBlankRowPreference(true); }, []); useEffect(() => { if (hasLoadedBlankRowPreference) writeBlankRowPreference(window.localStorage, blankRowCount); }, [blankRowCount, hasLoadedBlankRowPreference]);
  const downloadMarkdownTemplate = useCallback(() => { startLocalDownload(fileName("md"), PRIORITY_PLAN_MARKDOWN_TEMPLATE, "text/markdown;charset=utf-8"); showFeedback("Markdown template download started"); }, [fileName, showFeedback]);
  const downloadCsvTemplate = useCallback(() => { startLocalDownload(fileName("csv"), `\uFEFF${buildPriorityPlanCsv()}`, "text/csv;charset=utf-8"); showFeedback("CSV priority table download started"); }, [fileName, showFeedback]);
  const downloadBlankCsvTemplate = useCallback(() => { const rowCount = normalizeBlankRowCount(blankRowCount); startLocalDownload(fileName("csv", `-${rowCount}-blank-rows`), `\uFEFF${buildPriorityPlanCsvWithBlankRows(rowCount)}`, "text/csv;charset=utf-8"); showFeedback(`CSV with ${rowCount} blank priority rows download started`); }, [blankRowCount, fileName, showFeedback]);
  const copyMarkdownTemplate = useCallback(async () => { const copied = await copyTextToClipboard(PRIORITY_PLAN_MARKDOWN_TEMPLATE); showFeedback(copied ? "Markdown template copied to clipboard" : "Could not copy automatically — select the template text instead"); }, [showFeedback]);
  const copyPriorityTable = useCallback(async () => { const copied = await copyTextToClipboard(PRIORITY_PLAN_MARKDOWN_TABLE); showFeedback(copied ? "Priority table copied to clipboard" : "Could not copy automatically — select the table text instead"); }, [showFeedback]);
  const openPrintPreview = () => { setPrintDate(new Date()); setPrintPreviewOpen(true); };
  return <section className="priority-plan-template" id="priority-plan-template" aria-labelledby="priority-plan-template-title"><style>{styles}</style><div className="priority-plan-template-head"><div><span className="priority-plan-template-kicker"><FileSpreadsheet size={13} aria-hidden="true" /> Practical asset</span><h2 id="priority-plan-template-title">Download, preview, or print the research-to-priority plan.</h2><p>Choose an editable Excel workbook, a CSV priority table, or a plain Markdown record. All formats keep source-aware evidence separate from the decision, one modest priority, dependencies, review condition, and human check. Downloads stay in your browser and do not require an account.</p><span className="priority-plan-template-note">Excel includes: Start here, Priority plan, and Evidence notes worksheets. The CSV and print preview are simple priority-table records.</span></div><div className="priority-plan-template-actions"><label className="priority-plan-template-field">Project name (optional)<input type="text" maxLength={80} value={projectName} onChange={(event) => setProjectName(event.target.value)} placeholder="e.g. Q4 planning" aria-label="Project name" /></label><a className="priority-plan-template-button" href={PRIORITY_PLAN_TEMPLATE_URL} download={fileName("xlsx")} onClick={() => showFeedback("Excel template download started")}><Download size={15} aria-hidden="true" /> Download .xlsx</a><button type="button" className="priority-plan-template-button is-secondary" onClick={downloadCsvTemplate}><Download size={15} aria-hidden="true" /> Download .csv</button><label className="priority-plan-template-field">Blank CSV rows<select value={blankRowCount} onChange={(event) => setBlankRowCount(normalizeBlankRowCount(Number(event.target.value)))} aria-label="Blank CSV rows">{[3, 6, 10, 15, 25].map((count) => <option key={count} value={count}>{count} rows</option>)}</select></label><button type="button" className="priority-plan-template-button is-secondary" onClick={downloadBlankCsvTemplate}><Download size={15} aria-hidden="true" /> CSV · {blankRowCount} blank rows</button><label className="priority-plan-template-field">Print heading (optional)<input type="text" maxLength={80} value={printHeading} onChange={(event) => setPrintHeading(event.target.value)} placeholder="e.g. Q4 planning review" aria-label="Print heading" /></label><button type="button" className="priority-plan-template-button is-secondary" onClick={openPrintPreview}><Printer size={15} aria-hidden="true" /> Preview & print</button><button type="button" className="priority-plan-template-button is-secondary" onClick={downloadMarkdownTemplate}><FileText size={15} aria-hidden="true" /> Download .md</button><button type="button" className="priority-plan-template-button is-secondary" onClick={copyMarkdownTemplate}><Copy size={15} aria-hidden="true" /> Copy Markdown</button><button type="button" className="priority-plan-template-button is-secondary" onClick={copyPriorityTable}><Copy size={15} aria-hidden="true" /> Copy priority table</button></div></div>{feedback && <div className="priority-plan-template-toast" role="status" aria-live="polite"><Check size={16} aria-hidden="true" /> {feedback}</div>}{printPreviewOpen && printDate && <PriorityPlanPrintPreview onClose={() => setPrintPreviewOpen(false)} customHeading={printHeading.trim()} projectName={normalizedProjectName} printDate={printDate} />}</section>;
}
