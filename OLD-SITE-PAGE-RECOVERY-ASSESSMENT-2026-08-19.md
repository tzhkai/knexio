# 旧站页面恢复与迁移评估

**检查日期：** 2026-08-19  
**评估对象：** 旧版 `knexio.xyz` 页面与工具  
**当前主站：** Workflow Library

## 1. 结论

旧站确实存在一批真实的工具型页面，而不是只有文章页面。用户的判断方向是合理的：如果某些工具过去有真实使用价值，可以考虑恢复或重做。但目前没有足够证据证明“低跳出率”本身就能证明某个页面有真实用户，因为本次可访问的两个 GA4 资源对应的是 Chrome Web Store 扩展，而不是旧版 `knexio.xyz` 网站。

不过，旧站 Git 历史提供了另一项重要证据：旧版本确实加载过 GA4 Measurement ID `G-6VMQH2V72L`，并通过 `ga-analytics.js` 按页面类型记录 `tool_page`、`guide_page`、`game_page`、`finance_tool`、`business_tool` 和 `ai_tool`，同时记录工具使用、表单提交、文件导出、页面性能和会话事件。因此，旧站的历史 GA 数据理论上可以按 URL、页面类型和事件进一步验证；只是当前登录的 GA4 资源没有显示这个网站属性，不能把 Chrome Web Store 报告中的数据代替旧站数据。

最安全的策略不是恢复整个旧站，而是从备份中挑选少量**本地运行、无外部 API 依赖、与 Workflow Library 主题相容、能够提供独立解释内容**的工具，先以实验性工具区恢复。无关的游戏、泛计算器、设备故障文章和大量薄页面继续保持下线。

## 2. 旧站规模与页面类型

在旧仓库的稳定版本 `c150fb3e` 中，页面规模如下：

| 页面类别 | 数量 | 当前判断 |
|---|---:|---|
| `tools/` 工具 | 92 | 有少量值得恢复，大多数需要逐页审查 |
| `games/` 小游戏 | 51 | 与新 Workflow Library 主题无关，不建议恢复到主站 |
| `guides/` 故障排查文章 | 73 | 主题与新站不一致，不建议整体恢复 |
| `finance/` 工具 | 15 | 涉及金融/税务等高风险内容，暂不恢复 |
| `business/` 工具 | 13 | 需要逐项核对准确性和维护责任，暂不恢复 |
| `ai-tools/` 工具 | 21 | 只有在功能真实可用、内容非薄页时才考虑重做 |

旧站工具包括 `markdown-preview`、`ai-prompt-word-counter`、`text-analyzer`、`word-counter`、`meta-tag-generator`、`json-formatter`、`image-compressor`、`pdf-to-word-converter`、`screenshot-to-text`、`website-speed-test` 等。部分工具在浏览器本地完成处理，部分工具可能涉及文件、网络请求或浏览器能力，不能把所有页面直接复制回生产站。

## 3. 访问证据的可信度

本轮 GA4 核查得到的两个资源分别属于同名 Chrome Web Store 账号，页面标题为 `Knexio 阅读伴侣 - Chrome Web Store` 和 `knexio WebNote 网页摘要笔记 - Chrome Web Store`。第一个资源近 7 天只有 1 名 Singapore 活跃用户、1 次 Direct 会话和 1 次页面浏览；第二个资源近 7 天为 0。它们不能证明旧站工具页面有访问，也不能证明该 Singapore 访问是外部用户、站主调试或自动化流量。

| 证据 | 能否证明旧站工具有人使用 | 说明 |
|---|---:|---|
| Chrome Web Store GA4 页面有 1 次 Direct 会话 | 否 | 页面标题和资源身份不属于旧站工具 |
| 旧站源码曾使用 `G-6VMQH2V72L` | 只能证明曾部署跟踪 | 不能单独证明某个页面有用户 |
| 旧站 GA 事件函数包含 `tool_usage` | 具有验证价值 | 需要访问正确的 GA4 媒体资源读取事件参数 |
| 旧站工具有本地交互组件 | 只能证明功能存在 | 功能存在不等于真实使用量 |
| Search Console 历史曝光 | 只能证明搜索展示 | 曝光不等于点击或工具使用 |

因此，“低跳出率”只有在同时满足以下条件时才具有较高判断价值：页面 URL 确实是旧站工具路径；存在多个用户或多个会话；有 `tool_usage`、表单提交、文件导出或复制等事件；来源并非全部 Direct；访问时间分布不是集中在你调试的时间段；设备和国家分布也不是单一异常来源。

## 4. 推荐恢复候选

### 4.1 第一批：最适合迁移到 Workflow Library 的本地工具

| 候选页面 | 推荐处置 | 原因 | 迁移要求 |
|---|---|---|---|
| `ai-prompt-word-counter` | 优先重做 | 与 AI 提示词、上下文长度和工作流主题直接相关 | 明确“词数”与“token 数”不是同一概念；加入隐私说明和复制结果功能 |
| `markdown-preview` | 优先重做 | 适合研究、写作和笔记工作流 | 使用本地渲染；严格处理 HTML/XSS；加入 Markdown 写作说明 |
| `text-analyzer` / `word-counter` | 二选一重做 | 可服务写作工作流，但功能容易变成薄工具 | 合并为一个有解释内容的 Writing utility，避免两个重复页面争夺同一意图 |
| `meta-tag-generator` | 次优先 | 与网站发布、SEO 检查和内容工作流相关 | 明确它是生成草稿，不保证搜索排名；加入 TDK、Open Graph 使用指南 |
| `json-formatter` | 次优先 | 适合 API/结构化资料检查，但与主站主题联系较弱 | 放入“资料整理”工具区，补充示例、错误处理和隐私说明 |
| `image-compressor` | 观察后重做 | 有实际工具价值，但文件处理和性能边界更复杂 | 只在浏览器本地处理；说明文件不上传；限制文件大小和格式 |

第一批建议只选择 **3–5 个**，而不是把 92 个工具全部恢复。它们应在新站中作为“Workflow utilities”或“Small tools”子目录，并分别从相关指南链接进入。例如，`ai-prompt-word-counter` 可从提示词结构和写作指南进入，`markdown-preview` 可从 Writing 集群进入，`meta-tag-generator` 可从发布前检查指南进入。

### 4.2 暂不恢复的页面

51 个小游戏应继续下线。它们虽然可能有娱乐访问，但会把域名主题从 AI 工作流和实用指南拉向泛游戏站，不利于新站的主题一致性、AdSense 审核解释和维护效率。

73 个设备或应用故障排查指南也不建议整体恢复。它们与当前内容集群无关，且产品界面和故障步骤容易过时。如果历史数据明确显示某一页面仍有稳定搜索需求，应先单独重写为经过核验的现代指南，而不是恢复旧 HTML。

Finance 和 business 工具暂不恢复。涉及税务、贷款、投资、工资和商业决策的页面需要更高的准确性、更新时间和免责声明要求；在没有专门维护流程前，恢复这些页面会增加内容和合规风险。

PDF 转 Word、截图转文字、网站截图、网站速度测试等页面需要额外谨慎。它们可能依赖服务器、第三方 API、跨域权限或文件上传处理，不能仅因旧页面存在就直接上线。若要保留，应先确认文件是否上传、保存多久、是否有敏感信息处理以及生产环境是否具备相应后端。

## 5. 三种处置方式

### 方式 A：恢复原页面

只适合代码仍然完整、功能能在现代浏览器正常运行、旧 URL 有明确历史价值且页面内容可以重新审核的少数页面。恢复时应保留原 URL，补齐 canonical、robots、sitemap、隐私说明和当前品牌导航，并重新验证所有脚本和广告代码。不要直接把旧站整套静态目录复制到新站。

### 方式 B：迁移为新工具页

这是最推荐的方式。保留用户问题和工具价值，但用 Workflow Library 的设计、隐私策略、作者/更新时间、使用说明、限制条件和相关指南重新实现。旧 URL 如果与新工具路径一一对应，可以设置单次 301；如果没有明确对应关系，先让旧 URL 保持 404，等新页面真实上线后再建立映射。

### 方式 C：只保留功能，不恢复旧 SEO 页面

如果工具有实用价值但旧页面内容很薄、标题夸大或存在不安全实现，可以把功能作为新站某篇指南中的交互模块，而不是单独恢复旧页面。这种方式最适合简单的字数统计、提示词结构检查和 Markdown 预览等功能。

## 6. 推荐的页面级决策表

| 页面/类别 | 建议 | 立即动作 |
|---|---|---|
| `ai-prompt-word-counter` | 新站重做 | 先审计旧 JS，再设计隐私安全的本地工具 |
| `markdown-preview` | 新站重做 | 采用安全 Markdown 渲染，配写作指南 |
| `text-analyzer` 与 `word-counter` | 合并后重做 | 只保留一个 Writing utility，另一个不恢复 |
| `meta-tag-generator` | 第二批候选 | 与 SEO/发布流程指南一起规划 |
| `json-formatter` | 观察候选 | 只有在补足工作流场景后再发布 |
| `image-compressor` | 技术审计后决定 | 验证完全本地处理和文件大小边界 |
| PDF/OCR/网站截图/测速 | 暂不恢复 | 先确认第三方服务、上传和隐私风险 |
| 51 个小游戏 | 保持下线 | 不恢复到 knexio.xyz |
| 73 个故障排查指南 | 保持下线或逐页重写 | 不批量 301 到首页或 Workflow Library |
| Finance/Business 工具 | 保持下线 | 等建立专业维护和免责声明流程 |

## 7. 恢复前的验证流程

恢复前先从旧仓库固定一个只读备份，并记录旧页面文件、脚本、第三方依赖、广告代码和旧 GA 配置。随后在本地或预览域名测试工具的核心流程、异常输入、移动端、键盘操作、复制/下载行为和隐私边界。所有文件处理工具都要确认数据是否离开浏览器；任何外部服务调用都要明确服务商、用途、保留时间和失败处理。

上线时，每个页面必须返回 HTTP 200，canonical 指向最终 URL，robots 为 `index,follow`，只进入正确的 sitemap，并从至少一个相关指南和一个工具索引页获得自然内链。上线后先不批量请求索引，观察日志和真实访问，再对一两个核心页面使用 Search Console URL Inspection。

如果需要证明真实用户，不应只看跳出率。应同时记录页面浏览、工具启动、工具完成、复制结果、下载结果和错误事件，并排除站主调试流量。GA4 应使用专属 `knexio.xyz` 数据流；旧的 `G-6VMQH2V72L` 资源需要在 GA4 账号中确认是否仍可访问，不能用 Chrome Web Store 资源替代。

## 8. 最终建议

当前最稳妥的恢复路径是：**不恢复旧站整体；先从 `ai-prompt-word-counter`、`markdown-preview`、合并后的 Writing utility 中选择 2–3 个，在 Workflow Library 下重做；保留旧仓库作为备份；其余页面继续下线。**

如果正确的旧站 GA4 属性能够找回，再根据近 28 天或更长周期的页面级数据筛选候选：至少有多个用户/会话、存在工具使用事件、来源不全部为 Direct、访问时间不集中于调试时段，并且页面主题与新站一致。只有满足这些条件，才值得考虑保留旧 URL 或建立一对一 301。

## 参考记录

- 旧仓库稳定版本：`c150fb3e`
- 旧站 GA 配置：`ga-analytics.js`，Measurement ID `G-6VMQH2V72L`
- 当前 GA 核查记录：`GA4-LOW-BOUNCE-AUDIT-2026-08-19.md`
- 当前站内容集群方案：`KNEXIO-URL-CLEANUP-AND-CONTENT-CLUSTERS-2026-08-19.md`
