# Bridge Guide Assets and Reading Verification — 2026-08-25

The **Turn a research brief into a priority plan without hiding uncertainty** guide now includes a downloadable, editable Excel workbook. The workbook contains a start sheet, a priority-plan sheet with status validation and review checks, and an evidence-notes sheet that retains source, limitation, verification, and priority-impact fields. It was visually inspected after generation before upload to the project’s static asset storage.

The guide alone displays an accessible fixed-top reading progress bar. It calculates a clamped 0–100% article position, exposes `role="progressbar"` plus current value text, throttles scroll updates through `requestAnimationFrame`, and removes non-essential transitions under `prefers-reduced-motion`.

The new related-resources area provides two local tools and two directly connected guides. Desktop and 375px mobile screenshot review confirmed the template block, full-width mobile download control, resource-card stacking, page navigation, and article layout are readable without horizontal overflow. Production build prerendered 34 routes successfully.
